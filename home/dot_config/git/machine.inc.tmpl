# Generated by chezmoi; edit template data rather than this deployed file.
[user]
  name = {{ .name | quote }}
  email = {{ .email | quote }}
[ghq]
  root = {{ (joinPath .chezmoi.homeDir "src") | quote }}
{{ if .proxy -}}
[http]
  proxy = {{ .proxy | quote }}
{{ end -}}
