{{- /* macOS 専用のもの。将来 linux で apply したときに展開されないようにする */ -}}
{{ if ne .chezmoi.os "darwin" }}
Library/
.config/karabiner/
{{ end }}

{{- /*
   以下は ~/ に配置するファイルではなく、symlink_dot_* が指す実体。
   symlink にすることで、インストーラの `>> ~/.zshrc` や
   `git config --global` がリポジトリに直接書き戻る。
*/}}
zshrc
zprofile
gitconfig
