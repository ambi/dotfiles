[user]
	name = {{ .name }}
	email = {{ .email }}
[alias]
  co = checkout
[init]
  defaultBranch = main
[ghq]
  root = {{ .chezmoi.homeDir }}/src
{{ if .proxy }}[http]
  proxy = {{ .proxy }}
{{ end -}}
